// 4.Viết một hàm JavaScript để trích xuất một số ký tự cụ thể từ một chuỗi.
const truncate_string = "trường trung học phổ thông";
const startNum = 0;
const endsNum = 6;
const result = truncate_string.substring(startNum, endsNum);
console.log(result);
