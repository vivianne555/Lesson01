// Viết một hàm JavaScript để viết hoa ký tự đầu tiên trong một chuỗi.
const capitalize = "capitalize";
const captitalFirst = capitalize.substring(0,1)
const capitalOther = capitalize.substring(1,10);
const capitalTotal = captitalFirst.toUpperCase() + capitalOther
console.log (capitalTotal);
