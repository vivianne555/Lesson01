// 1. Viết một hàm JavaScript để chuyển đổi chuỗi Hexadecimal thành chuỗi ASCII.
// Dữ liệu kiểm tra:

```jsx
console.log(hex_to_ascii('3132'));
console.log(hex_to_ascii('313030'));
```

// Kết quả:

```arduino
"12"
"100"
```